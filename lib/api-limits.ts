/** Shared request caps kept outside route modules so Next.js route contracts
 * contain only supported HTTP/config exports. */
export const MAX_CONTACT_BULK = 200;
export const MAX_DEAL_BULK = 200;
export const MAX_CONTACT_MERGE_GROUP = 50;
