{
  "name": "chippi-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Always-on background worker: BullMQ queues over Redis. Runs recurring jobs and agentic task offload for the Chippi app.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.56.0",
    "ioredis": "^5.6.1",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
